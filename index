<!DOCTYPE html>
<html lang="th">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Smart Water Pump</title>

<link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500&display=swap" rel="stylesheet">

<style>
body {
    font-family: 'Kanit', sans-serif;
    background: #f6edff;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    margin: 0;
}

/* ===== กล่องหลัก ===== */
.container {
    width: 380px;
    background: #ffffff;
    padding: 25px;
    border-radius: 25px;
    box-shadow: 0 10px 25px rgba(0,0,0,0.08);
    text-align: center;
}

/* ===== Header ===== */
.header {
    background: #8fe3df;
    border: 2px dashed #444;
    border-radius: 50px;
    padding: 12px 30px;
    display: inline-flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 25px;
}

.header h1 {
    font-size: 22px;
    margin: 0;
    color: #222;
}

/* ===== Status Bar ===== */
.status-bar {
    display: flex;
    align-items: center;
    padding: 14px 20px;
    border-radius: 16px;
    font-size: 18px;
    font-weight: 500;
    margin-bottom: 15px;
}

.icon {
    font-size: 22px;
    margin-right: 12px;
}

.value-right {
    margin-left: auto;
    font-weight: bold;
}

/* สีตามภาพ */
.moisture {
    background: #e6b4f2;
}

.pump {
    background: #8be05f;
}

.mode {
    background: #18c0df;
}

/* ===== Switch ===== */
.controls {
    display: flex;
    justify-content: space-between;
    margin-top: 30px;
}

.switch-group {
    text-align: center;
}

.switch-label {
    font-size: 16px;
    margin-bottom: 8px;
}

/* Toggle Button */
.toggle {
    width: 120px;
    height: 48px;
    background: linear-gradient(90deg, #a06cff, #6ee6ff);
    border-radius: 50px;
    position: relative;
    display: flex;
    align-items: center;
    padding: 4px;
    box-shadow: inset 0 0 6px rgba(0,0,0,0.2);
}

.toggle-circle {
    width: 38px;
    height: 38px;
    background: radial-gradient(circle, #4facfe, #00f2fe);
    border-radius: 50%;
    position: absolute;
    left: 5px;
}

.toggle-text {
    width: 100%;
    text-align: right;
    padding-right: 16px;
    font-size: 20px;
    font-weight: bold;
}

.sub-label {
    font-size: 12px;
    color: #666;
    margin-top: 5px;
}
</style>
</head>

<body>

<div class="container">

    <!-- Header -->
    <div class="header">
        <h1>Smart Water Pump</h1>
        <span>🌲</span>
    </div>

    <!-- Moisture -->
    <div class="status-bar moisture">
        <div class="icon">💧</div>
        <span>ค่าความชื้น</span>
        <span class="value-right">45%</span>
    </div>

    <!-- Pump Status -->
    <div class="status-bar pump">
        <div class="icon">🚰</div>
        <span>สถานะปั๊ม</span>
        <span class="value-right">ON</span>
    </div>

    <!-- Mode -->
    <div class="status-bar mode">
        <div class="icon">⚙️</div>
        <span>โหมด</span>
        <span class="value-right">AUTO</span>
    </div>

    <!-- Switch -->
    <div class="controls">

        <div class="switch-group">
            <div class="switch-label">ระบบ</div>
            <div class="toggle">
                <div class="toggle-circle"></div>
                <div class="toggle-text">ON</div>
            </div>
            <div class="sub-label">on / off</div>
        </div>

        <div class="switch-group">
            <div class="switch-label">โหมด Auto</div>
            <div class="toggle">
                <div class="toggle-circle"></div>
                <div class="toggle-text">ON</div>
            </div>
            <div class="sub-label">on / off</div>
        </div>

    </div>
</div>

</body>
</html>
